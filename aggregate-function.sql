--Find the cumulative money of all the sellers
select sum(smoney) from seller;

-- Find the total number of users
select count(*) from userss

-- Find the average of starting bid of items table
SELECT AVG(startBid) 
FROM items;

